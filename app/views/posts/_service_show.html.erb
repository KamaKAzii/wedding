<% if user.posts.any? %>

  <h2>Posts</h2>

  <ul>
    <%= user.posts.each do |p| %>
      <li>
        <%= p.title %> on <%= p.date %>
        <%= p.body %>
      </li>
    <% end %>
  </ul>

<% end %>

